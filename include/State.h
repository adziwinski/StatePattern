#ifndef STATE_H
#define STATE_H

#include "StateMachine.h"
#include "State.h"

class State
{
    public:

        State();
        virtual ~State();

//        virtual run(StateMachine * currentState) = 0;

    protected:

    private:

};

#endif // STATE_H
